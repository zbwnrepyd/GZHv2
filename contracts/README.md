# Contracts

`contracts/` 存放跨模块共享的数据契约，当前包括：

- `fields.json`：系统可用字段的类型、分组、必填状态和枚举/数值约束。字段不天然绑定到某张卡片。
- `media.json`：图片素材类型、采集/生成属性和默认必填状态。图片素材不天然绑定到某张卡片。

维护约定：

- 修改契约时同步检查定稿台、图片定稿台、排版中心和测试里的字段/素材引用。
- 不在这里存放真实公司数据、生成图片、数据库文件或运行时缓存。
